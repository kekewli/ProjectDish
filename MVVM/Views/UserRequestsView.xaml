<Window x:Class="ProjectDish.MVVM.Views.UserRequestsView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:vm="clr-namespace:ProjectDish.MVVM.ViewModels"
        xmlns:cv="clr-namespace:ProjectDish.Core.Converters"
        mc:Ignorable="d"
        Title="Запросы пользователей" Height="600" Width="800"
        WindowStartupLocation="CenterScreen"
        WindowStyle="None" Background="Transparent" AllowsTransparency="True">

    <Window.DataContext>
        <vm:UserRequestsViewModel/>
    </Window.DataContext>

    <Window.Resources>
        <BooleanToVisibilityConverter x:Key="BoolToVisConverter"/>
        <cv:InverseBooleanConverter x:Key="InverseBoolConverter"/>

        <Style TargetType="Button" x:Key="ActionButton">
            <Setter Property="Background" Value="#FF2E7D32"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="Padding" Value="15,8"/>
            <Setter Property="Margin" Value="5"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="border" CornerRadius="8" Background="{TemplateBinding Background}">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="border" Property="Background" Value="#FF388E3C"/>
                            </Trigger>
                            <Trigger Property="IsEnabled" Value="False">
                                <Setter TargetName="border" Property="Background" Value="#FFE0E0E0"/>
                                <Setter Property="Foreground" Value="#FFA0A0A0"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </Window.Resources>

    <Border CornerRadius="20" Background="White" Margin="10">
        <Border.Effect>
            <DropShadowEffect BlurRadius="20" ShadowDepth="0" Opacity="0.2"/>
        </Border.Effect>

        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>

            <!-- Заголовок -->
            <StackPanel Grid.Row="0" Margin="25,20,25,10">
                <TextBlock Text="Запросы на добавление рецептов" FontSize="20" FontWeight="Bold" Foreground="#FF2E7D32"/>
                <Separator Margin="0,10,0,0"/>
            </StackPanel>

            <!-- Список -->
            <ListBox Grid.Row="1" ItemsSource="{Binding Requests}" 
                     SelectedItem="{Binding SelectedRequest}"
                     ScrollViewer.HorizontalScrollBarVisibility="Disabled"
                     BorderThickness="0" Background="Transparent" Margin="15">
                <ListBox.ItemsPanel>
                    <ItemsPanelTemplate>
                        <StackPanel Orientation="Vertical"/>
                    </ItemsPanelTemplate>
                </ListBox.ItemsPanel>
                <ListBox.ItemTemplate>
                    <DataTemplate>
                        <Border Width="700" Height="100" Margin="5" Background="#FAFAFA" CornerRadius="10" BorderBrush="#EEE" BorderThickness="1">
                            <Grid>
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="90"/>
                                    <ColumnDefinition Width="*"/>
                                    <ColumnDefinition Width="Auto"/>
                                </Grid.ColumnDefinitions>

                                <!-- Изображение -->
                                <Border Grid.Column="0" Margin="8" CornerRadius="8" Background="#EEE">
                                    <Image Source="{Binding ImageUrl}" Stretch="UniformToFill">
                                        <Image.Clip>
                                            <RectangleGeometry Rect="0,0,74,84" RadiusX="8" RadiusY="8"/>
                                        </Image.Clip>
                                    </Image>
                                </Border>

                                <!-- Информация -->
                                <StackPanel Grid.Column="1" Margin="10,10,0,10" VerticalAlignment="Center">
                                    <TextBlock Text="{Binding RecipeName}" FontWeight="Bold" FontSize="16" TextTrimming="CharacterEllipsis"/>
                                    <TextBlock Margin="0,5,0,0" Foreground="Gray">
                                        <!--<Run Text="От пользователя: "/>
                                        <Run Text="{Binding UserName}" FontWeight="SemiBold" Foreground="#FF2E7D32"/>-->
                                    </TextBlock>
                                </StackPanel>

                                <!-- Кнопка просмотра -->
                                <Button Grid.Column="2" Content="👁"
                                    ToolTip="Просмотреть детали"
                                    Command="{Binding DataContext.OpenDetailsCommand, RelativeSource={RelativeSource AncestorType=Window}}"
                                    CommandParameter="{Binding}"
                                    Width="50" Height="50" Margin="10" VerticalAlignment="Center"
                                    Background="Transparent" BorderThickness="0" FontSize="24" Cursor="Hand">
                                    <Button.Style>
                                        <Style TargetType="Button">
                                            <Setter Property="Foreground" Value="Gray"/>
                                            <Style.Triggers>
                                                <Trigger Property="IsMouseOver" Value="True">
                                                    <Setter Property="Foreground" Value="#FF2E7D32"/>
                                                </Trigger>
                                            </Style.Triggers>
                                        </Style>
                                    </Button.Style>
                                </Button>
                            </Grid>
                        </Border>
                    </DataTemplate>
                </ListBox.ItemTemplate>
                <ListBox.ItemContainerStyle>
                    <Style TargetType="ListBoxItem">
                        <Setter Property="Template">
                            <Setter.Value>
                                <ControlTemplate TargetType="ListBoxItem">
                                    <Border x:Name="border" BorderThickness="2" BorderBrush="Transparent" CornerRadius="12" Margin="2">
                                        <ContentPresenter/>
                                    </Border>
                                    <ControlTemplate.Triggers>
                                        <Trigger Property="IsSelected" Value="True">
                                            <Setter TargetName="border" Property="BorderBrush" Value="#FF2E7D32"/>
                                            <Setter TargetName="border" Property="Background" Value="#E8F5E9"/>
                                        </Trigger>
                                        <Trigger Property="IsMouseOver" Value="True">
                                            <Setter TargetName="border" Property="Background" Value="#F5F5F5"/>
                                        </Trigger>
                                    </ControlTemplate.Triggers>
                                </ControlTemplate>
                            </Setter.Value>
                        </Setter>
                    </Style>
                </ListBox.ItemContainerStyle>
            </ListBox>

            <!-- Кнопки действий -->
            <Border Grid.Row="2" Padding="15" BorderThickness="0,1,0,0" BorderBrush="#EEE">
                <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
                    <Button Content="Одобрить" Command="{Binding ApproveRequestCommand}" 
                            Style="{StaticResource ActionButton}" Width="135" Height="30"/>
                    <Button Content="Отклонить" Command="{Binding RejectRequestCommand}" 
                            Style="{StaticResource ActionButton}" Background="#D32F2F" Margin="10,0,20,0" Width="100" Height="30"/>
                    <Button Content="Закрыть" 
                         Command="{Binding CloseCommand}" 
                         CommandParameter="{Binding RelativeSource={RelativeSource AncestorType={x:Type Window}}}" 
                         Background="Transparent" BorderThickness="0" Foreground="Gray" FontSize="14" Cursor="Hand" Padding="10,5">
                        <Button.Template>
                            <ControlTemplate TargetType="Button">
                                <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                            </ControlTemplate>
                        </Button.Template>
                        <Button.Style>
                            <Style TargetType="Button">
                                <Setter Property="Foreground" Value="Gray"/>
                                <Style.Triggers>
                                    <Trigger Property="IsMouseOver" Value="True">
                                        <Setter Property="Foreground" Value="#FF2E7D32"/>
                                    </Trigger>
                                </Style.Triggers>
                            </Style>
                        </Button.Style>
                    </Button>
                </StackPanel>
            </Border>

            <!-- Индикатор загрузки -->
            <Grid Grid.RowSpan="3" Background="#80FFFFFF" Visibility="{Binding IsBusy, Converter={StaticResource BoolToVisConverter}}">
                <Border Background="White" CornerRadius="10" Padding="30" HorizontalAlignment="Center" VerticalAlignment="Center">
                    <Border.Effect>
                        <DropShadowEffect BlurRadius="20" Opacity="0.2"/>
                    </Border.Effect>
                    <StackPanel>
                        <TextBlock Text="⏳" FontSize="40" HorizontalAlignment="Center" Margin="0,0,0,10"/>
                        <TextBlock Text="Загрузка..." FontSize="18" FontWeight="Bold" Foreground="#FF2E7D32"/>
                    </StackPanel>
                </Border>
            </Grid>

            <!-- Сообщение если пусто -->
            <StackPanel Grid.Row="1" VerticalAlignment="Center" HorizontalAlignment="Center"
                        Visibility="{Binding Requests.Count, Converter={StaticResource BoolToVisConverter}, ConverterParameter=Invert}">
                <TextBlock Text="📭" FontSize="48" HorizontalAlignment="Center" Foreground="#CCC"/>
                <TextBlock Text="Нет запросов на рассмотрение" FontSize="16" Foreground="#999" HorizontalAlignment="Center" Margin="0,10,0,0"/>
            </StackPanel>
        </Grid>
    </Border>
</Window>