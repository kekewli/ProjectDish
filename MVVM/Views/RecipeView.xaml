<Window x:Class="ProjectDish.MVVM.Views.RecipeView"
         xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:vm="clr-namespace:ProjectDish.MVVM.ViewModels"
        mc:Ignorable="d"
        Title="{Binding WindowTitle}" Height="519" Width="900"
        WindowStartupLocation="CenterScreen"
        ResizeMode="NoResize"
        Background="White">

    <Window.Resources>
        <!-- Стили (можно вынести в App.xaml для переиспользования) -->
        <Style TargetType="TextBox">
            <Setter Property="Padding" Value="5"/>
            <Setter Property="Margin" Value="0,5,0,15"/>
        </Style>
        <Style TargetType="TextBlock">
            <Setter Property="Foreground" Value="Gray"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
        </Style>
    </Window.Resources>

    <!-- Передаем окно как параметр в команду Cancel -->
    <Window.InputBindings>
        <KeyBinding Key="Esc" Command="{Binding CancelCommand}" CommandParameter="{Binding RelativeSource={RelativeSource AncestorType=Window}}"/>
    </Window.InputBindings>

    <Grid Margin="20">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="300"/>
            <!-- Картинка -->
            <ColumnDefinition Width="*"/>
            <!-- Поля -->
        </Grid.ColumnDefinitions>

        <!-- Левая колонка: Картинка -->
        <StackPanel Grid.Column="0" Margin="0,0,20,0">
            <TextBlock Text="Изображение" Margin="0,0,0,5"/>

            <Border Height="300" 
                    BorderBrush="#FF2E7D32" 
                    BorderThickness="2" 
                    CornerRadius="10" 
                    AllowDrop="True"
                    Drop="Border_Drop"
                    Background="#FAFAFA"
                    Cursor="Hand">
                <Border.InputBindings>
                    <MouseBinding MouseAction="LeftClick" Command="{Binding UploadImageCommand}"/>
                </Border.InputBindings>

                <Grid>
                    <!-- Подсказка -->
                    <StackPanel VerticalAlignment="Center" HorizontalAlignment="Center">
                        <TextBlock Text="📷" FontSize="40" HorizontalAlignment="Center" Foreground="#CCC"/>
                        <TextBlock Text="Нажмите или перетащите" Foreground="#CCC" HorizontalAlignment="Center" Margin="0,10,0,0"/>
                    </StackPanel>

                    <!-- Сама картинка -->
                    <Image Source="{Binding ImagePath}" Stretch="UniformToFill">
                        <Image.Clip>
                            <RectangleGeometry Rect="0,0,296,296" RadiusX="8" RadiusY="8"/>
                        </Image.Clip>
                    </Image>
                </Grid>
            </Border>

            <Button Content="Выбрать файл..." Command="{Binding UploadImageCommand}" Margin="0,10,0,0"/>
        </StackPanel>

        <!-- Правая колонка: Поля ввода -->
        <Grid Grid.Column="1">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
                <RowDefinition Height="*"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>

            <!-- Название -->
            <StackPanel Grid.Row="0">
                <TextBlock Text="Название рецепта"/>
                <TextBox Text="{Binding Name, UpdateSourceTrigger=PropertyChanged}" FontSize="16"/>
            </StackPanel>

            <!-- Категория -->
            <StackPanel Grid.Row="1">
                <TextBlock Text="Категория"/>
                <ComboBox ItemsSource="{Binding Categories}" 
                          SelectedItem="{Binding SelectedCategory}"
                          DisplayMemberPath="Name"
                          Height="35" Margin="0,5,0,15" Padding="5"/>
            </StackPanel>

            <!-- Описание -->
            <StackPanel Grid.Row="2">
                <TextBlock Text="Описание"/>
                <TextBox Text="{Binding Description, UpdateSourceTrigger=PropertyChanged}" 
                         TextWrapping="Wrap" AcceptsReturn="True" VerticalScrollBarVisibility="Auto"
                         Height="100"/>
            </StackPanel>

            <!-- Ингредиенты -->
            <StackPanel Grid.Row="3">
                <TextBlock Text="Ингредиенты"/>
                <TextBox Text="{Binding Ingredients, UpdateSourceTrigger=PropertyChanged}" 
                         TextWrapping="Wrap" AcceptsReturn="True" VerticalScrollBarVisibility="Auto"
                         Height="100"/>
            </StackPanel>

            <!-- Кнопки -->
            <StackPanel Grid.Row="4" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,20,0,0">
                <Button Content="Отмена" 
                        Command="{Binding CancelCommand}" 
                        CommandParameter="{Binding RelativeSource={RelativeSource AncestorType=Window}}"
                        Width="100" Height="35" Background="Transparent" BorderBrush="Gray" BorderThickness="1" Margin="0,0,10,0"/>

                <Button Content="Сохранить" 
                        Command="{Binding SaveCommand}"
                        Width="120" Height="35" Background="#FF2E7D32" Foreground="White" FontWeight="Bold" 
                        IsEnabled="{Binding IsBusy, Converter={StaticResource InverseBoolConverter}}"/>
                <!-- Нужен конвертер или просто триггер -->
            </StackPanel>
        </Grid>

        <!-- Индикатор загрузки (overlay) -->
        <Grid Grid.ColumnSpan="2" Background="#80FFFFFF" Visibility="{Binding IsBusy, Converter={StaticResource BoolToVisConverter}}">
            <TextBlock Text="Сохранение..." HorizontalAlignment="Center" VerticalAlignment="Center" FontSize="20" FontWeight="Bold" Foreground="#FF2E7D32"/>
        </Grid>
    </Grid>
</Window>