<Window x:Class="ProjectDish.MVVM.Views.RegisterView"
         xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:vm="clr-namespace:ProjectDish.MVVM.ViewModels"
        mc:Ignorable="d"
        Title="Регистрация" Height="550" Width="400"
        WindowStartupLocation="CenterScreen"
        ResizeMode="NoResize"
        WindowStyle="None"
        Background="Transparent"
        AllowsTransparency="True">

    <Window.DataContext>
        <vm:RegisterViewModel/>
    </Window.DataContext>

    <Window.Resources>
        <!-- Стили остаются те же, что я присылал выше -->
        <Style TargetType="TextBox">
            <Setter Property="Padding" Value="0,5"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="BorderThickness" Value="0,0,0,1"/>
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="BorderBrush" Value="#FFAAAAAA"/>
            <Setter Property="VerticalContentAlignment" Value="Center"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="TextBox">
                        <Border BorderBrush="{TemplateBinding BorderBrush}" BorderThickness="{TemplateBinding BorderThickness}" Background="{TemplateBinding Background}">
                            <ScrollViewer x:Name="PART_ContentHost"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsFocused" Value="True">
                                <Setter Property="BorderBrush" Value="#FF2E7D32"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style TargetType="PasswordBox">
            <Setter Property="Padding" Value="0,5"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="BorderThickness" Value="0,0,0,1"/>
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="BorderBrush" Value="#FFAAAAAA"/>
            <Setter Property="VerticalContentAlignment" Value="Center"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="PasswordBox">
                        <Border BorderBrush="{TemplateBinding BorderBrush}" BorderThickness="{TemplateBinding BorderThickness}" Background="{TemplateBinding Background}">
                            <ScrollViewer x:Name="PART_ContentHost"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsFocused" Value="True">
                                <Setter Property="BorderBrush" Value="#FF2E7D32"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style TargetType="Button">
            <Setter Property="Background" Value="#FF2E7D32"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="Padding" Value="10"/>
            <Setter Property="Margin" Value="0,5"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="border" CornerRadius="5" Background="{TemplateBinding Background}">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="border" Property="Background" Value="#FF388E3C"/>
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter TargetName="border" Property="Background" Value="#FF1B5E20"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </Window.Resources>

    <Border CornerRadius="20" Background="White" Margin="10">
        <Border.Effect>
            <DropShadowEffect BlurRadius="15" ShadowDepth="0" Opacity="0.2"/>
        </Border.Effect>
        <Border.Triggers>
            <EventTrigger RoutedEvent="Loaded">
                <BeginStoryboard>
                    <Storyboard>
                        <DoubleAnimation Storyboard.TargetProperty="Opacity" From="0" To="1" Duration="0:0:0.5"/>
                        <ThicknessAnimation Storyboard.TargetProperty="Margin" From="10,30,10,-10" To="10" Duration="0:0:0.5" DecelerationRatio="0.9"/>
                    </Storyboard>
                </BeginStoryboard>
            </EventTrigger>
        </Border.Triggers>

        <Grid Margin="40">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>

            <!-- Заголовок -->
            <StackPanel Grid.Row="0" Margin="0,0,0,30">
                <TextBlock Text="Регистрация" FontSize="26" FontWeight="Bold" Foreground="#FF2E7D32" HorizontalAlignment="Center"/>
                <TextBlock Text="Создайте новый аккаунт" FontSize="14" Foreground="Gray" HorizontalAlignment="Center" Margin="0,5,0,0"/>
            </StackPanel>

            <!-- Поля ввода -->
            <StackPanel Grid.Row="1" VerticalAlignment="Center">

                <TextBlock Text="Имя пользователя" Foreground="Gray" FontSize="12" Margin="0,0,0,5"/>
                <TextBox Text="{Binding UserName, UpdateSourceTrigger=PropertyChanged}" Margin="0,0,0,20"/>

                <TextBlock Text="Email" Foreground="Gray" FontSize="12" Margin="0,0,0,5"/>
                <TextBox Text="{Binding Email, UpdateSourceTrigger=PropertyChanged}" Margin="0,0,0,20"/>

                <TextBlock Text="Пароль" Foreground="Gray" FontSize="12" Margin="0,0,0,5"/>

                <!-- БЛОК С ПАРОЛЕМ И ГЛАЗОМ -->
                <Grid Margin="0,0,0,5">
                    <!-- Поле пароля (скрытое по умолчанию) -->
                    <PasswordBox x:Name="RegPasswordBox" Margin="0,0,35,0"/>

                    <!-- Поле текста (для показа пароля) -->
                    <TextBox x:Name="VisiblePasswordBox" Margin="0,0,35,0" 
                             Visibility="Collapsed"
                             TextChanged="VisiblePasswordBox_TextChanged"/>

                    <!-- Кнопка переключения -->
                    <ToggleButton x:Name="TogglePassBtn" 
                                  Content="👁" 
                                  HorizontalAlignment="Right" VerticalAlignment="Center"
                                  Width="30" Height="30"
                                  Background="Transparent" BorderThickness="0"
                                  Click="TogglePassBtn_Click" Cursor="Hand"/>
                </Grid>

                <TextBlock Text="* Минимум 8 символов, буквы и цифры" FontSize="10" Foreground="Gray" FontStyle="Italic" Margin="0,0,0,20"/>
            </StackPanel>

            <!-- Кнопки -->
            <StackPanel Grid.Row="2" Margin="0,20,0,0">
                <!-- Важно: CommandParameter передает именно RegPasswordBox -->
                <Button Content="Зарегистрироваться" 
                        Command="{Binding RegisterCommand}" 
                        CommandParameter="{Binding ElementName=RegPasswordBox}"
                        Height="45"/>

                <Button Content="У меня уже есть аккаунт" 
                        Command="{Binding NavigateToLoginCommand}"
                        Background="Transparent" 
                        Foreground="#FF2E7D32" 
                        BorderBrush="#FF2E7D32" 
                        BorderThickness="1"
                        Margin="0,10,0,0">
                    <Button.Template>
                        <ControlTemplate TargetType="Button">
                            <Border x:Name="b" CornerRadius="5" BorderBrush="{TemplateBinding BorderBrush}" BorderThickness="1" Background="Transparent">
                                <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                            </Border>
                            <ControlTemplate.Triggers>
                                <Trigger Property="IsMouseOver" Value="True">
                                    <Setter TargetName="b" Property="Background" Value="#EEFFF0"/>
                                </Trigger>
                            </ControlTemplate.Triggers>
                        </ControlTemplate>
                    </Button.Template>
                </Button>

                <Button Content="Выход" 
                        Command="{Binding ExitCommand}" 
                        Background="Transparent" 
                        Foreground="Gray" 
                        FontSize="12"
                        Margin="0,15,0,0">
                    <Button.Template>
                        <ControlTemplate TargetType="Button">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </ControlTemplate>
                    </Button.Template>
                </Button>
            </StackPanel>
        </Grid>
    </Border>
</Window>