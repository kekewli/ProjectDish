<Window x:Class="ProjectDish.MVVM.Views.RequestDetailsView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        mc:Ignorable="d"
        Title="Детали запроса" Height="600" Width="950"
        WindowStartupLocation="CenterScreen"
        ResizeMode="NoResize"
        WindowStyle="None" Background="Transparent" AllowsTransparency="True">

    <Window.Resources>
        <BooleanToVisibilityConverter x:Key="BoolToVisConverter"/>

        <!-- Стиль кнопки Закрыть -->
        <Style TargetType="Button" x:Key="CloseButtonStyle">
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="Foreground" Value="Gray"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Padding" Value="10,5"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Foreground" Value="#FF2E7D32"/>
                </Trigger>
            </Style.Triggers>
        </Style>

        <!-- Стиль для TextBox -->
        <Style TargetType="TextBox" x:Key="ReadOnlyTextBox">
            <Setter Property="Background" Value="#FDFDFD"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Padding" Value="5"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="TextWrapping" Value="Wrap"/>
            <Setter Property="IsReadOnly" Value="True"/>
            <Setter Property="VerticalScrollBarVisibility" Value="Auto"/>
        </Style>
    </Window.Resources>

    <Grid>
        <Border CornerRadius="20" Background="White" Margin="10">
            <Border.Effect>
                <DropShadowEffect BlurRadius="20" ShadowDepth="0" Opacity="0.2"/>
            </Border.Effect>
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>

                <!-- Заголовок -->
                <Grid Grid.Row="0" Margin="25,20,25,10">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="Auto"/>
                        <ColumnDefinition Width="*"/>
                    </Grid.ColumnDefinitions>
                    <StackPanel Grid.Column="1">
                        <TextBlock Text="{Binding RecipeName}" FontSize="24" FontWeight="Bold" Foreground="#FF2E7D32" TextWrapping="Wrap"/>
                        <TextBlock Text="{Binding CategoryName}" FontSize="16" Foreground="Gray" Margin="0,5,0,0"/>
                    </StackPanel>
                </Grid>
                <Separator Grid.Row="0" VerticalAlignment="Bottom" Margin="25,0"/>

                <!-- Контент  -->
                <ScrollViewer Grid.Row="1" VerticalScrollBarVisibility="Auto">
                    <Grid Margin="25,20,25,20">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="280"/>
                            <ColumnDefinition Width="*"/>
                        </Grid.ColumnDefinitions>

                        <!-- Картинка -->
                        <StackPanel Grid.Column="0" Margin="0,0,20,0">
                            <Border CornerRadius="10" Height="220" Background="#F0F0F0">
                                <Image Source="{Binding ImageUrl, TargetNullValue='pack://application:,,,/Resources/placeholder.png'}" Stretch="UniformToFill"/>
                            </Border>
                        </StackPanel>

                        <!-- Описание и Ингредиенты  -->
                        <StackPanel Grid.Column="1">
                            <TextBlock Text="Описание" FontWeight="SemiBold" FontSize="16"/>
                            <TextBox Text="{Binding Description}" Margin="0,5,0,20" Style="{StaticResource ReadOnlyTextBox}" MinHeight="180"/>

                            <TextBlock Text="Ингредиенты" FontWeight="SemiBold" FontSize="16"/>
                            <TextBox Text="{Binding Ingredients}" Margin="0,5,0,0" Style="{StaticResource ReadOnlyTextBox}" MinHeight="180"/>
                        </StackPanel>
                    </Grid>
                </ScrollViewer>

                <!-- Нижняя панель с кнопкой -->
                <Border Grid.Row="2" Padding="15" BorderThickness="0,1,0,0" BorderBrush="#EEE">
                    <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
                        <Button Content="Закрыть" 
                        Command="{Binding CloseCommand}" 
                        CommandParameter="{Binding RelativeSource={RelativeSource AncestorType={x:Type Window}}}" 
                        Background="Transparent" BorderThickness="0" Foreground="Gray" FontSize="14" Cursor="Hand" Padding="10,5">
                            <Button.Template>
                                <ControlTemplate TargetType="Button">
                                    <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                </ControlTemplate>
                            </Button.Template>
                            <Button.Style>
                                <Style TargetType="Button">
                                    <Setter Property="Foreground" Value="Gray"/>
                                    <Style.Triggers>
                                        <Trigger Property="IsMouseOver" Value="True">
                                            <Setter Property="Foreground" Value="#FF2E7D32"/>
                                        </Trigger>
                                    </Style.Triggers>
                                </Style>
                            </Button.Style>
                        </Button>
                    </StackPanel>
                </Border>

                <!-- Индикатор загрузки  -->
                <Grid Grid.RowSpan="3" Background="#80FFFFFF" Visibility="{Binding IsBusy, Converter={StaticResource BoolToVisConverter}}" Panel.ZIndex="20">
                    <Border Background="White" CornerRadius="10" Padding="20" HorizontalAlignment="Center" VerticalAlignment="Center">
                        <Border.Effect>
                            <DropShadowEffect BlurRadius="20" Opacity="0.2"/>
                        </Border.Effect>
                        <StackPanel>
                            <TextBlock Text="⏳" FontSize="30" HorizontalAlignment="Center" Margin="0,0,0,10"/>
                            <TextBlock Text="Загрузка..." FontWeight="Bold" Foreground="#FF2E7D32"/>
                        </StackPanel>
                    </Border>
                </Grid>
            </Grid>
        </Border>
    </Grid>
</Window>