<Window x:Class="ProjectDish.MVVM.Views.AdminView"
         xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:vm="clr-namespace:ProjectDish.MVVM.ViewModels"
        mc:Ignorable="d"
        Title="Панель администратора" Height="700" Width="1000"
        WindowStartupLocation="CenterScreen"
        Background="#F5F5F5">

    <Window.DataContext>
        <vm:AdminViewModel/>
    </Window.DataContext>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <!-- Верхняя панель -->
            <RowDefinition Height="*"/>
            <!-- Список рецептов -->
            <RowDefinition Height="Auto"/>
            <!-- Нижняя панель действий -->
        </Grid.RowDefinitions>

        <!-- Верхняя панель (Поиск и фильтры) -->
        <Border Grid.Row="0" Background="White" Padding="20" BorderBrush="#DDD" BorderThickness="0,0,0,1">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto"/>
                    <!-- Лого/Текст -->
                    <ColumnDefinition Width="*"/>
                    <!-- Поиск -->
                    <ColumnDefinition Width="Auto"/>
                    <!-- Сортировка -->
                    <ColumnDefinition Width="Auto"/>
                    <!-- Выход -->
                </Grid.ColumnDefinitions>

                <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                    <TextBlock Text="Project Dishes" FontWeight="Bold" FontSize="18" Foreground="#FF2E7D32"/>
                    <TextBlock Text=" | Admin" FontSize="18" Foreground="Gray"/>
                </StackPanel>

                <!-- Поиск -->
                <TextBox Grid.Column="1" Margin="30,0" Padding="10,8" FontSize="14" VerticalAlignment="Center"
                         Text="{Binding SearchText, UpdateSourceTrigger=PropertyChanged}">
                    <TextBox.Resources>
                        <Style TargetType="Border">
                            <Setter Property="CornerRadius" Value="5"/>
                        </Style>
                    </TextBox.Resources>
                    <TextBox.Style>
                        <Style TargetType="TextBox">
                            <Setter Property="Template">
                                <Setter.Value>
                                    <ControlTemplate TargetType="TextBox">
                                        <Border Background="#F0F0F0" CornerRadius="15" Padding="5">
                                            <Grid>
                                                <Grid.ColumnDefinitions>
                                                    <ColumnDefinition Width="Auto"/>
                                                    <ColumnDefinition Width="*"/>
                                                </Grid.ColumnDefinitions>
                                                <TextBlock Text="🔍" Margin="10,0,5,0" VerticalAlignment="Center" Foreground="Gray"/>
                                                <ScrollViewer x:Name="PART_ContentHost" Grid.Column="1" VerticalAlignment="Center"/>
                                                <TextBlock Text="Поиск рецептов..." Grid.Column="1" VerticalAlignment="Center" Foreground="Gray" IsHitTestVisible="False">
                                                    <TextBlock.Style>
                                                        <Style TargetType="TextBlock">
                                                            <Setter Property="Visibility" Value="Collapsed"/>
                                                            <Style.Triggers>
                                                                <DataTrigger Binding="{Binding Text, RelativeSource={RelativeSource TemplatedParent}}" Value="">
                                                                    <Setter Property="Visibility" Value="Visible"/>
                                                                </DataTrigger>
                                                            </Style.Triggers>
                                                        </Style>
                                                    </TextBlock.Style>
                                                </TextBlock>
                                            </Grid>
                                        </Border>
                                    </ControlTemplate>
                                </Setter.Value>
                            </Setter>
                        </Style>
                    </TextBox.Style>
                </TextBox>

                <!-- Сортировка -->
                <ComboBox Grid.Column="2" Width="200" Padding="10" VerticalAlignment="Center" Margin="0,0,20,0"
                          SelectedIndex="{Binding SelectedSortIndex}">
                    <ComboBoxItem Content="Сортировка: По умолчанию"/>
                    <ComboBoxItem Content="Сортировка: Рейтинг (Высокий)"/>
                    <ComboBoxItem Content="Сортировка: Рейтинг (Низкий)"/>
                    <ComboBoxItem Content="Сортировка: А-Я"/>
                </ComboBox>

                <Button Grid.Column="3" Content="Выйти" Command="{Binding LogoutCommand}" 
                        Padding="15,8" Background="Transparent" BorderThickness="1" BorderBrush="#FFD32F2F" Foreground="#FFD32F2F"/>
            </Grid>
        </Border>

        <!-- Список рецептов -->
        <ScrollViewer Grid.Row="1" Padding="15">
            <ListBox ItemsSource="{Binding Recipes}" 
                     SelectedItem="{Binding SelectedRecipe}"
                     ScrollViewer.HorizontalScrollBarVisibility="Disabled"
                     BorderThickness="0" Background="Transparent">

                <!-- Панель для размещения элементов (Плитка) -->
                <ListBox.ItemsPanel>
                    <ItemsPanelTemplate>
                        <WrapPanel Orientation="Horizontal" HorizontalAlignment="Center"/>
                    </ItemsPanelTemplate>
                </ListBox.ItemsPanel>

                <!-- Шаблон карточки рецепта -->
                <ListBox.ItemTemplate>
                    <DataTemplate>
                        <Border Width="280" Height="140" Margin="10" Background="White" CornerRadius="10">
                            <Border.Effect>
                                <DropShadowEffect BlurRadius="10" ShadowDepth="2" Opacity="0.1"/>
                            </Border.Effect>
                            <Grid>
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="110"/>
                                    <ColumnDefinition Width="*"/>
                                </Grid.ColumnDefinitions>

                                <!-- Картинка -->
                                <Border Grid.Column="0" Margin="10" CornerRadius="8" Background="#EEE">
                                    <Border.OpacityMask>
                                        <VisualBrush>
                                            <VisualBrush.Visual>
                                                <Border Background="Black" Width="{Binding ActualWidth, RelativeSource={RelativeSource AncestorType=Border}}" Height="{Binding ActualHeight, RelativeSource={RelativeSource AncestorType=Border}}" CornerRadius="8"/>
                                            </VisualBrush.Visual>
                                        </VisualBrush>
                                    </Border.OpacityMask>
                                    <Image Source="{Binding ImageUrl, IsAsync=True, TargetNullValue=/Images/placeholder.png}" Stretch="UniformToFill"/>
                                </Border>

                                <!-- Информация -->
                                <StackPanel Grid.Column="1" Margin="0,15,10,10">
                                    <TextBlock Text="{Binding Name}" FontWeight="Bold" FontSize="14" TextWrapping="Wrap" MaxHeight="60" TextTrimming="CharacterEllipsis"/>

                                    <StackPanel Orientation="Horizontal" Margin="0,10,0,0">
                                        <TextBlock Text="★" Foreground="#FFC107"/>
                                        <TextBlock Text="{Binding Rating, StringFormat=N1}" Foreground="Gray" Margin="5,0,0,0"/>
                                    </StackPanel>

                                    <!-- Кнопка "Подробнее" внутри карточки -->
                                    <Button Content="Подробнее" 
                                            Command="{Binding DataContext.OpenDetailsCommand, RelativeSource={RelativeSource AncestorType=Window}}"
                                            CommandParameter="{Binding Id}"
                                            HorizontalAlignment="Left" Margin="0,10,0,0"
                                            Background="#E8F5E9" Foreground="#2E7D32" BorderThickness="0" Padding="10,5" FontSize="12">
                                        <Button.Resources>
                                            <Style TargetType="Border">
                                                <Setter Property="CornerRadius" Value="4"/>
                                            </Style>
                                        </Button.Resources>
                                    </Button>
                                </StackPanel>
                            </Grid>
                        </Border>
                    </DataTemplate>
                </ListBox.ItemTemplate>

                <ListBox.ItemContainerStyle>
                    <Style TargetType="ListBoxItem">
                        <Setter Property="Template">
                            <Setter.Value>
                                <ControlTemplate TargetType="ListBoxItem">
                                    <Border x:Name="border" BorderThickness="2" BorderBrush="Transparent" CornerRadius="12" Margin="2">
                                        <ContentPresenter/>
                                    </Border>
                                    <ControlTemplate.Triggers>
                                        <Trigger Property="IsSelected" Value="True">
                                            <Setter TargetName="border" Property="BorderBrush" Value="#2E7D32"/>
                                            <Setter TargetName="border" Property="Background" Value="#F1F8E9"/>
                                        </Trigger>
                                        <Trigger Property="IsMouseOver" Value="True">
                                            <Setter TargetName="border" Property="Background" Value="#FAFAFA"/>
                                        </Trigger>
                                    </ControlTemplate.Triggers>
                                </ControlTemplate>
                            </Setter.Value>
                        </Setter>
                    </Style>
                </ListBox.ItemContainerStyle>
            </ListBox>
        </ScrollViewer>

        <!-- Нижняя панель действий -->
        <Border Grid.Row="2" Background="White" Padding="15" BorderBrush="#DDD" BorderThickness="0,1,0,0">
            <StackPanel Orientation="Horizontal" HorizontalAlignment="Center">

                <Button Command="{Binding AddRecipeCommand}" Content="➕ Добавить рецепт" 
                        Background="#FF2E7D32" Foreground="White" Padding="15,10" Margin="5"/>

                <Button Command="{Binding EditRecipeCommand}" Content="✎ Изменить" 
                        Background="#FFF9A825" Foreground="White" Padding="15,10" Margin="5"/>

                <Button Command="{Binding DeleteRecipeCommand}" Content="🗑 Удалить" 
                        Background="#FFD32F2F" Foreground="White" Padding="15,10" Margin="5"/>

                <Border Width="1" Height="30" Background="Gray" Margin="15,0"/>

                <Button Command="{Binding OpenRequestsCommand}" Content="Запросы пользователей" 
                        Background="White" BorderBrush="Gray" BorderThickness="1" Padding="15,10" Margin="5"/>

                <Button Command="{Binding OpenUsersCommand}" Content="Пользователи" 
                        Background="White" BorderBrush="Gray" BorderThickness="1" Padding="15,10" Margin="5"/>
            </StackPanel>
        </Border>
    </Grid>
</Window>