<Window x:Class="ProjectDish.MVVM.Views.AdminView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:vm="clr-namespace:ProjectDish.MVVM.ViewModels"
        mc:Ignorable="d"
        Title="Панель администратора" Height="700" Width="1000"
        WindowStartupLocation="CenterScreen"
        ResizeMode="NoResize"
        WindowStyle="None"
        Background="Transparent"
        AllowsTransparency="True">

    <Window.DataContext>
        <vm:AdminViewModel/>
    </Window.DataContext>

    <Window.Resources>
        <!-- СТИЛЬ ДЛЯ КНОПОК ДЕЙСТВИЙ -->
        <Style TargetType="Button" x:Key="ActionButton">
            <Setter Property="Background" Value="#FF2E7D32"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="Padding" Value="15,8"/>
            <Setter Property="Margin" Value="5"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="border" CornerRadius="8" Background="{TemplateBinding Background}">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="border" Property="Background" Value="#FF388E3C"/>
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter TargetName="border" Property="Background" Value="#FF1B5E20"/>
                            </Trigger>
                            <Trigger Property="IsEnabled" Value="False">
                                <Setter TargetName="border" Property="Background" Value="#FFE0E0E0"/>
                                <Setter Property="Foreground" Value="#FFA0A0A0"/>
                                <Setter Property="Cursor" Value="Arrow"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style TargetType="Button" x:Key="OutlineButton">
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="Foreground" Value="Gray"/>
            <Setter Property="BorderBrush" Value="#FFAAAAAA"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="Padding" Value="15,8"/>
            <Setter Property="Margin" Value="5"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="border" CornerRadius="8" BorderBrush="{TemplateBinding BorderBrush}" BorderThickness="{TemplateBinding BorderThickness}" Background="{TemplateBinding Background}">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="border" Property="BorderBrush" Value="#FF2E7D32"/>
                                <Setter Property="Foreground" Value="#FF2E7D32"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style TargetType="ComboBox">
            <Setter Property="Padding" Value="10,5"/>
            <Setter Property="Background" Value="White"/>
            <Setter Property="BorderBrush" Value="#DDD"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="VerticalContentAlignment" Value="Center"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="ComboBox">
                        <Border x:Name="templateBorder" CornerRadius="8" BorderBrush="{TemplateBinding BorderBrush}" BorderThickness="{TemplateBinding BorderThickness}" Background="{TemplateBinding Background}">
                            <Grid>
                                <ToggleButton Name="ToggleButton" 
                                              BorderBrush="{TemplateBinding BorderBrush}" 
                                              Background="{TemplateBinding Background}"
                                              IsChecked="{Binding Path=IsDropDownOpen,Mode=TwoWay,RelativeSource={RelativeSource TemplatedParent}}"
                                              Grid.Column="2" ClickMode="Press" Focusable="false"
                                              HorizontalAlignment="Stretch" VerticalAlignment="Stretch">
                                    <ToggleButton.Template>
                                        <ControlTemplate>
                                            <Border Background="Transparent">
                                                <Path Data="M0,0 L10,10 L20,0" Stroke="Gray" StrokeThickness="1" HorizontalAlignment="Right" Margin="0,0,10,0" VerticalAlignment="Center"/>
                                            </Border>
                                        </ControlTemplate>
                                    </ToggleButton.Template>
                                </ToggleButton>
                                <ContentPresenter Name="ContentSite" IsHitTestVisible="False"  Content="{TemplateBinding SelectionBoxItem}"
                                                  ContentTemplate="{TemplateBinding SelectionBoxItemTemplate}"
                                                  ContentTemplateSelector="{TemplateBinding ItemTemplateSelector}"
                                                  Margin="10,0,30,0"
                                                  VerticalAlignment="Center"
                                                  HorizontalAlignment="Left" />
                                <Popup Name="Popup" Placement="Bottom" IsOpen="{TemplateBinding IsDropDownOpen}" AllowsTransparency="True" Focusable="False" PopupAnimation="Slide">
                                    <Grid Name="DropDown" SnapsToDevicePixels="True" MinWidth="{TemplateBinding ActualWidth}" MaxHeight="{TemplateBinding MaxDropDownHeight}">
                                        <Border x:Name="DropDownBorder" Background="White" BorderThickness="1" BorderBrush="#DDD" CornerRadius="5"/>
                                        <ScrollViewer Margin="4,6,4,6" SnapsToDevicePixels="True">
                                            <StackPanel IsItemsHost="True" KeyboardNavigation.DirectionalNavigation="Contained" />
                                        </ScrollViewer>
                                    </Grid>
                                </Popup>
                            </Grid>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </Window.Resources>

    <Border CornerRadius="20" Background="#F9F9F9" Margin="10">
        <Border.Effect>
            <DropShadowEffect BlurRadius="20" ShadowDepth="0" Opacity="0.2"/>
        </Border.Effect>
        <Border.Triggers>
            <EventTrigger RoutedEvent="Loaded">
                <BeginStoryboard>
                    <Storyboard>
                        <DoubleAnimation Storyboard.TargetProperty="Opacity" From="0" To="1" Duration="0:0:0.5"/>
                        <ThicknessAnimation Storyboard.TargetProperty="Margin" From="10,30,10,-10" To="10" Duration="0:0:0.5" DecelerationRatio="0.9"/>
                    </Storyboard>
                </BeginStoryboard>
            </EventTrigger>
        </Border.Triggers>

        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <!-- Верхняя панель -->
                <RowDefinition Height="*"/>
                <!-- Список рецептов -->
                <RowDefinition Height="Auto"/>
                <!-- Нижняя панель действий -->
            </Grid.RowDefinitions>

            <!-- Верхняя панель -->
            <Border Grid.Row="0" Background="White" Padding="20" CornerRadius="20,20,0,0" BorderBrush="#EEE" BorderThickness="0,0,0,1">
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="Auto"/>
                        <!-- Лого -->
                        <ColumnDefinition Width="*"/>
                        <!-- Поиск -->
                        <ColumnDefinition Width="Auto"/>
                        <!-- Сортировка -->
                        <ColumnDefinition Width="Auto"/>
                        <!-- Выход -->
                    </Grid.ColumnDefinitions>

                    <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                        <TextBlock Text="Project Dishes" FontWeight="Bold" FontSize="18" Foreground="#FF2E7D32"/>
                        <TextBlock Text=" | Admin" FontSize="18" Foreground="Gray"/>
                    </StackPanel>

                    <!-- Поиск -->
                    <TextBox Grid.Column="1" Margin="30,0" Padding="10,8" FontSize="14" VerticalAlignment="Center"
                             Text="{Binding SearchText, UpdateSourceTrigger=PropertyChanged}">
                        <TextBox.Style>
                            <Style TargetType="TextBox">
                                <Setter Property="Template">
                                    <Setter.Value>
                                        <ControlTemplate TargetType="TextBox">
                                            <Border Background="#F5F5F5" CornerRadius="20" Padding="5">
                                                <Grid>
                                                    <Grid.ColumnDefinitions>
                                                        <ColumnDefinition Width="Auto"/>
                                                        <ColumnDefinition Width="*"/>
                                                    </Grid.ColumnDefinitions>
                                                    <TextBlock Text="🔍" Margin="10,0,5,0" VerticalAlignment="Center" Foreground="Gray"/>
                                                    <ScrollViewer x:Name="PART_ContentHost" Grid.Column="1" VerticalAlignment="Center"/>

                                                    <TextBlock Text="Поиск рецептов..." Grid.Column="1" VerticalAlignment="Center" Foreground="Gray" IsHitTestVisible="False">
                                                        <TextBlock.Style>
                                                            <Style TargetType="TextBlock">
                                                                <Setter Property="Visibility" Value="Collapsed"/>
                                                                <Style.Triggers>
                                                                    <!-- Показывать только если текста нет И фокуса нет -->
                                                                    <MultiDataTrigger>
                                                                        <MultiDataTrigger.Conditions>
                                                                            <Condition Binding="{Binding Text, RelativeSource={RelativeSource TemplatedParent}}" Value=""/>
                                                                            <Condition Binding="{Binding IsKeyboardFocused, RelativeSource={RelativeSource TemplatedParent}}" Value="False"/>
                                                                        </MultiDataTrigger.Conditions>
                                                                        <Setter Property="Visibility" Value="Visible"/>
                                                                    </MultiDataTrigger>
                                                                </Style.Triggers>
                                                            </Style>
                                                        </TextBlock.Style>
                                                    </TextBlock>
                                                </Grid>
                                            </Border>
                                        </ControlTemplate>
                                    </Setter.Value>
                                </Setter>
                            </Style>
                        </TextBox.Style>
                    </TextBox>

                    <!-- Сортировка -->
                    <ComboBox Grid.Column="1" Width="200" VerticalAlignment="Center" Margin="498,0,20,0"
                              SelectedIndex="{Binding SelectedSortIndex}" Height="24">
                        <ComboBoxItem Content="Сортировка: По умолчанию"/>
                        <ComboBoxItem Content="Сортировка: Рейтинг (Высокий)"/>
                        <ComboBoxItem Content="Сортировка: Рейтинг (Низкий)"/>
                        <ComboBoxItem Content="Сортировка: А-Я"/>
                    </ComboBox>

                    <!-- Кнопка выхода -->
                    <Button Grid.Column="3" Content="Выйти" Command="{Binding LogoutCommand}" 
                            Background="Transparent" BorderThickness="0" Foreground="#FFD32F2F" Cursor="Hand" FontWeight="SemiBold">
                        <Button.Template>
                            <ControlTemplate TargetType="Button">
                                <Border Background="Transparent">
                                    <ContentPresenter/>
                                </Border>
                            </ControlTemplate>
                        </Button.Template>
                    </Button>
                </Grid>
            </Border>

            <!-- Список рецептов -->
            <ScrollViewer Grid.Row="1" Padding="15">
                <ListBox ItemsSource="{Binding Recipes}" 
                         SelectedItem="{Binding SelectedRecipe}"
                         ScrollViewer.HorizontalScrollBarVisibility="Disabled"
                         BorderThickness="0" Background="Transparent">

                    <ListBox.ItemsPanel>
                        <ItemsPanelTemplate>
                            <WrapPanel Orientation="Horizontal" HorizontalAlignment="Center"/>
                        </ItemsPanelTemplate>
                    </ListBox.ItemsPanel>

                    <ListBox.ItemTemplate>
                        <DataTemplate>
                            <Border Width="280" Height="140" Margin="10" Background="White" CornerRadius="12">
                                <Border.Effect>
                                    <DropShadowEffect BlurRadius="10" ShadowDepth="2" Opacity="0.08"/>
                                </Border.Effect>
                                <Grid>
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="110"/>
                                        <ColumnDefinition Width="*"/>
                                    </Grid.ColumnDefinitions>

                                    <!-- Картинка -->
                                    <Border Grid.Column="0" Margin="10" CornerRadius="8" Background="#F8F8F8">
                                        <Border.Clip>
                                            <RectangleGeometry Rect="0,0,90,120" RadiusX="8" RadiusY="8"/>
                                        </Border.Clip>
                                        <Grid>
                                            <TextBlock Text="📷" HorizontalAlignment="Center" VerticalAlignment="Center" Foreground="#DDD" FontSize="20"/>
                                            <Image Source="{Binding ImageUrl, TargetNullValue={x:Null}}" Stretch="UniformToFill"/>
                                        </Grid>
                                    </Border>

                                    <!-- Информация -->
                                    <StackPanel Grid.Column="1" Margin="0,15,10,10">
                                        <TextBlock Text="{Binding Name}" FontWeight="Bold" FontSize="14" TextWrapping="Wrap" MaxHeight="40" TextTrimming="CharacterEllipsis"/>

                                        <StackPanel Orientation="Horizontal" Margin="0,5,0,0">
                                            <TextBlock Text="★" Foreground="#FFC107"/>
                                            <TextBlock Text="{Binding Rating, StringFormat=N1}" Foreground="Gray" Margin="5,0,0,0"/>
                                        </StackPanel>

                                        <!-- Кнопка "Подробнее" внутри карточки -->
                                        <Button Content="Подробнее" 
                                                Command="{Binding DataContext.OpenDetailsCommand, RelativeSource={RelativeSource AncestorType=Window}}"
                                                CommandParameter="{Binding Id}"
                                                HorizontalAlignment="Left" Margin="0,15,0,0"
                                                Background="#F1F8E9" Foreground="#2E7D32" BorderThickness="0" Padding="12,4" FontSize="11" FontWeight="SemiBold" Cursor="Hand">
                                            <Button.Resources>
                                                <Style TargetType="Border">
                                                    <Setter Property="CornerRadius" Value="10"/>
                                                </Style>
                                            </Button.Resources>
                                        </Button>
                                    </StackPanel>
                                </Grid>
                            </Border>
                        </DataTemplate>
                    </ListBox.ItemTemplate>

                    <ListBox.ItemContainerStyle>
                        <Style TargetType="ListBoxItem">
                            <Setter Property="Template">
                                <Setter.Value>
                                    <ControlTemplate TargetType="ListBoxItem">
                                        <Border x:Name="border" BorderThickness="2" BorderBrush="Transparent" CornerRadius="14" Margin="2">
                                            <ContentPresenter/>
                                        </Border>
                                        <ControlTemplate.Triggers>
                                            <Trigger Property="IsSelected" Value="True">
                                                <Setter TargetName="border" Property="BorderBrush" Value="#2E7D32"/>
                                                <Setter TargetName="border" Property="Background" Value="#F1F8E9"/>
                                            </Trigger>
                                            <Trigger Property="IsMouseOver" Value="True">
                                                <Setter TargetName="border" Property="Background" Value="#FFFFFF"/>
                                                <Setter TargetName="border" Property="BorderBrush" Value="#E0E0E0"/>
                                            </Trigger>
                                        </ControlTemplate.Triggers>
                                    </ControlTemplate>
                                </Setter.Value>
                            </Setter>
                        </Style>
                    </ListBox.ItemContainerStyle>
                </ListBox>
            </ScrollViewer>

            <!-- Нижняя панель действий -->
            <Border Grid.Row="2" Background="White" Padding="15" CornerRadius="0,0,20,20" BorderBrush="#EEE" BorderThickness="0,1,0,0">
                <StackPanel Orientation="Horizontal" HorizontalAlignment="Center">

                    <!-- Кнопка Добавить  -->
                    <Button Command="{Binding AddRecipeCommand}" Content="➕ Добавить" 
                            Style="{StaticResource ActionButton}" Width="115" Height="30"/>

                    <!-- Кнопки Изменить/Удалить -->
                    <Button Command="{Binding EditRecipeCommand}" Content="✎ Изменить" 
                            Style="{StaticResource ActionButton}" Width="115" Height="30"/>

                    <Button Command="{Binding DeleteRecipeCommand}" Content="🗑 Удалить" 
                            Style="{StaticResource ActionButton}" Background="#D32F2F" Width="100" Height="30">
                        <!-- Переопределение цвета для удаления -->
                        <Button.Template>
                            <!-- Дублируем шаблон для красной кнопки, чтобы сохранить триггеры -->
                            <ControlTemplate TargetType="Button">
                                <Border x:Name="border" CornerRadius="8" Background="{TemplateBinding Background}">
                                    <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                </Border>
                                <ControlTemplate.Triggers>
                                    <Trigger Property="IsMouseOver" Value="True">
                                        <Setter TargetName="border" Property="Background" Value="#B71C1C"/>
                                    </Trigger>
                                    <Trigger Property="IsEnabled" Value="False">
                                        <Setter TargetName="border" Property="Background" Value="#FFE0E0E0"/>
                                        <Setter Property="Foreground" Value="#FFA0A0A0"/>
                                    </Trigger>
                                </ControlTemplate.Triggers>
                            </ControlTemplate>
                        </Button.Template>
                    </Button>

                    <Border Width="1" Height="30" Background="#EEE" Margin="20,0"/>

                    <!-- Второстепенные кнопки -->
                    <Button Command="{Binding OpenRequestsCommand}" Content="Запросы" 
                            Style="{StaticResource OutlineButton}" Width="78" Height="30"/>

                    <Button Command="{Binding OpenUsersCommand}" Content="Пользователи" 
                            Style="{StaticResource OutlineButton}" Height="30" Width="130"/>
                </StackPanel>
            </Border>
        </Grid>
    </Border>
</Window>